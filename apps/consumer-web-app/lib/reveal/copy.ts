/**
 * Root Presence System (Prompt 4), requirement 3: the one shared, Root-
 * voiced "putting this together" line, previously hand-duplicated
 * verbatim in three separate takers (Core Values Snapshot, Life Signal
 * Check, Readiness Pulse) for the same `beat === 'finishing'` transition
 * into "What Root learned." Centralized here so a future fourth taker
 * imports this instead of copy-pasting a fourth string, and so a future
 * wording change only has one place to make it.
 */

export const ROOT_FINISHING_LABEL = "One moment, I'm putting this together.";
